// import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => (
  <Container>
    <Typography variant="h3">Welcome to the Weather App</Typography>
    <Typography variant="body1">Use this app to get weather information for your favorite cities!</Typography>
  </Container>
);

export default Home;
